% looking at data formats

clear all
close all
clc

%% load in Intan data
cd('G:\My Drive\lab-private\lab-teaching\2020_techniques\lecture_2');

fid = fopen('amplifier_analogin_auxiliary_int16.dat', 'rb');

raw_data = int16(fread(fid, Inf, 'int16'));

fclose(fid);


%% fixing it up
nChan = 7;
nice_data = reshape(raw_data, nChan, length(raw_data)/nChan);

timevec = 1:1/20000:
plot(nice_data(2,1:20000))
